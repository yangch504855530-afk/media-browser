# Media Browser v1.4.2 删除功能优化

## 日期

2026-06-04

## 问题

Windows 播放视频时，源文件可能被浏览器连接或 `/play` 实时转码进程占用，导致删除失败。

## 修改内容

### 后端

- 按媒体路径追踪活跃的实时转码 `ffmpeg` 进程。
- 删除媒体前终止并等待读取该文件的实时转码进程退出。
- Windows 遇到 `PermissionError` 时按 `0.3s`、`0.5s` 延迟重试。
- 单文件删除、删除整个作品、废纸篓重试和废纸篓批量删除统一使用安全删除逻辑。

### 前端

- 删除当前视频前，主动暂停并清除 `<video>` 的 `src`，断开 HTTP 连接。
- 删除当前作品前，若该作品正在画廊播放，同样先释放视频连接。

## 验证重点

- Windows 上播放 `.avi`、`.ts`、`.mts` 等实时转码格式时直接删除。
- Windows 上播放 MP4 等浏览器直出格式时直接删除。
- 删除失败时仍正确加入废纸篓队列。

## 后续安全加固

- 默认仅监听 `127.0.0.1`；局域网监听强制配置 `MB_ACCESS_TOKEN`。
- 禁用跨域请求并限制 JSON 请求体大小。
- 修复 `/thumb` 路径穿越。
- 符号链接扫描限制在扫描根内并检测目录循环。
- play-ready 后台转码纳入进程释放，并为转码缓存增加删除清理与容量上限。
- 单文件删除增加确认。
