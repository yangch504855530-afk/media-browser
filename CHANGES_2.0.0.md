# Media Browser v2.0.0

## 定位升级

Media Browser 从媒体浏览与基础审阅工具升级为个人视频资产筛选工具。原有扫描、缩略图、画廊、播放、分析任务和删除能力保持兼容。

## 新增

- 每个视频支持独立的 1–5 星个人评分以及保留、待定、待删除状态；文件夹只负责组织展示。
- Ollama 完成单视频分析后，自动保存该视频的分类和结构化特征，无需用户手工打标签。
- 若配置的 Ollama 模型未安装，会自动选择本机已有的视觉模型（例如 `qwen2.5vl:7b`），避免任务启动后没有分类结果。
- AI 任务会显示 `analyzing` / `analyzed` / `analyzed_with_errors` 的真实状态；Qwen-VL 自动使用更稳妥的抽帧数和超时设置。
- AI 状态按视频持久化为 `queued` / `running` / `done` / `error`；应用重启后仅恢复未完成视频，并持久化上次使用的扫描目录。
- 应用并扫描后自动为每个未完成视频创建独立、串行执行的任务，无需手工勾选或点击分析。
- 自动分类 schema v2 使用固定维度：人物数量、场景、制作类型、镜头风格、叙事程度、显著特征和置信度，并要求 Ollama 输出严格 JSON。
- 自动分类 schema v3 增加演员候选、制作方和作品编号；演员必须由“文件路径+画面文字”交叉确认，或命中本地别名库，禁止仅凭人脸猜姓名。
- 自动分类 schema v4 增加音频语言与地区来源：本地 faster-whisper 对短音频采样识别语种，再结合直属目录/文件名、片头文字、水印、制作方和编号，归入俄罗斯、欧美、国产、日本、韩国、东南亚、其他或不确定。
- 地区判断禁止依据人物脸部、肤色或外貌；每条结果保存地区置信度和可核对依据，证据不足时保留“不确定”。
- 分析页改为“自动接受 + 异常复核”：高置信度且主要分类完整的视频直接生效，只把中低置信度、失败或缺失结果列给用户处理。
- 新增分类进度总览、紧凑复核卡片、批量接受/重试/排除，以及 A/R/X 键盘快捷键；逐字段编辑、任务和重命名收进高级详情。
- 演员候选单独提示，不再阻塞场景、人数、制作类型等其他分类；异常处理状态按视频持久化。
- 健康检查对大型缓存目录采用有界统计，避免数万缓存文件导致 `/health` 请求超时。
- 审阅账本格式升级至 v2，并按稳定视频标识保存 `ai_analysis`、`features` 和 `categories`。
- 新增 `GET /api/preferences/summary`，汇总评分、分类和特征，提供透明的个人分类依据。
- 审阅侧栏新增“我的偏好与分类依据”。

## 数据兼容与安全

- v1 审阅账本自动兼容，下一次保存时升级为 v2，无需重新标记。
- “待删除”仍是可复核状态；只有用户明确执行“永久删除”才会移除源文件。
- 新增资料保存在当前媒体库对应的缓存账本中，不修改媒体源文件。

## 接口

- `PATCH /api/review-state/video/{id}` 保存单视频评分、状态及机器生成的分析资料。
- `GET /api/preferences/summary` 返回审阅计数、评分分布、分类统计和特征值统计。
