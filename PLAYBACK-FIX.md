# v1.5.0 播放兼容性修复

基线：yangch504855530-afk/media-browser 的 v1.5.0，提交 02b423df16f255db74851a03c6b6599ead325c56。

## 修复

- Windows 同时查找 `_internal` 和程序所在目录中的 ffmpeg.exe/ffprobe.exe；源码运行也支持同目录工具。原版只在内部目录查找不带 .exe 的文件，遗漏随 Windows 安装包发布的工具。
- MP4/MOV 根据视频编码选择兼容播放；HEVC、MPEG-4 Part 2 等不再被容器后缀强制直放。
- MP4 包括 H.264 在内直放失败时，自动尝试 H.264/AAC 缓存播放。回退后重新加载媒体能继续自动播放；再次失败显示错误，不无限重试。
- TS 原有扩展名扫描支持保留，通过修复后的工具路径生成缩略图和兼容播放缓存。

## 使用

双击 Start-MediaBrowser.cmd，浏览器打开 http://127.0.0.1:8766/ 。
启动需要当前机器已有的 Python；不是重新打包的 EXE。原 v1.5.0 安装目录保持原样。
默认扫描 F:\Video，缓存和配置独立存放于本目录 runtime，不修改媒体源文件。
首轮扫描与不兼容视频的首次转换可能需要等待；缓存完成后可以直接播放、拖动。

## 验证

- 29 项播放相关测试通过，包含真实 Chromium 播放 TS 转换结果、不兼容 MP4 转换结果、H.264 直放失败后的回退，验证有画面、时间前进及源文件 SHA-256 不变。
- F:\Video 抽查 12 个 TS、12 个 MP4，24 个均可读取视频信息。
- 各取一个实际 TS/MP4 的 5 秒片段在独立缓存目录完成兼容转换，源文件大小和修改时间不变。没有声称全部原始文件均可播放或损坏文件可恢复。
- 原版在当前 Windows 环境也失败的 6 项权限相关测试：3 项依赖符号链接创建权限；3 项依赖 POSIX 目录只读语义。与本次播放修改无关，未改动删除功能。

完整首轮回归为 103 通过、7 失败：其中一个旧播放策略断言已同步修改，并在上述 29 项相关测试中通过；另 6 项已在未修改 v1.5.0 上复现。后续扩大回归两次长时间无进展，已停止，不能宣称完整套件全绿。运行版首页和作品接口实测均 HTTP 200。

## 按需预览更新

TS 默认使用按需预览：每段 6 秒，最多提前加载下一段，拖动时直接请求目标段。浏览器不支持此方式或片段异常时回退完整兼容播放；已有完整缓存继续复用。
预览宽度最多 1280 像素、H.264/AAC，仅缓存预览副本，原件保持不变。为避免 TS 跳转丢失关键帧，目标段最多向前预读 30 秒；异常片段拒绝缓存并回退。
关闭画廊/切换视频会中止前端加载和定时器；已开始的单段服务端任务最多完成当前段，不会继续全片处理。缓存按现有播放缓存容量限制清理。
新增浏览器验证包含有声/无声 TS 连播、跳至第 25 秒、回拖、结尾、释放后停止请求，确认不请求完整转换且不生成跳过的中间片段；相关 7 项测试通过，另 1 项路径边界测试通过。
实际库中一个 TS 抽查：首段约 0.477 秒，第 60 秒所在段约 1.159 秒，已缓存片段接近即时返回。这是本机单样本处理耗时，不保证所有视频或磁盘同样快。

### 连续播放预读修复

按需预览开始后维持约 18 秒的播放缓冲，暂停时保留约 12 秒。每个片段加入浏览器后立即继续准备后续片段，不再等到当前 6 秒片段只剩 2 秒时才开始。拖动进度仍以新的播放位置为优先目标。

## 缓存管理

页面顶部“缓存管理”提供当前目录总占用、完整播放缓存、按需片段、缩略图的文件数和占用，可分别清理或清理全部可再生成缓存。
审阅账本、删除失败记录、重命名映射、分析数据及未知文件不参与清理。仅删除识别的缓存文件，不递归删除目录、不跟随链接；目录切换后旧清理请求失效。最近一分钟写入的文件保留，缩略图清理需等扫描完成。清理缩略图后请重新扫描生成。
3 项测试通过，包括浏览器确认清理、保留记录和未知文件、近期文件保护、扫描状态保护及过期目录标记拒绝。本次部署没有执行用户真实缓存清理。
