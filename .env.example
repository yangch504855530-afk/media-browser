# Media Browser Docker 部署环境变量示例
# 复制为 .env 并按你的 NAS 修改；也可不创建 .env，直接在命令行注入这些变量。
# 所有路径/ GID 变量均有默认值（作者当前 NAS 布局），不填也能直接 docker compose up。
# 注意：.env 含访问令牌等敏感信息，已被 .gitignore 忽略，请勿提交。

# 缩略图/转码缓存宿主机目录（容器内挂到 /cache）
MB_CACHE_HOST_DIR=/vol1/1000/handle-pri/media-browser/cache

# 媒体库：tmp 审阅目录（容器内挂到 /scan/tmp）
MB_TMP_HOST_DIR=/vol3/1000/handle-3T-cun3/tmp

# 媒体库：自动下载目录（容器内挂到 /scan/torrent）
MB_TORRENT_HOST_DIR=/vol3/1000/handle-3T-cun3/torrent/auto-down

# Intel/AMD 核显 render 组 GID（宿主机用 getent group render 查看）
MB_RENDER_GID=105

# 局域网认证（非本机监听时必填）：访问令牌与账号密码二选一。
# 令牌方式：先用 openssl rand -hex 24 生成后填入
# MB_ACCESS_TOKEN=你的随机令牌

# 账号密码方式（HTTP Basic，兼容 NAS 现有登录习惯；不要提交真实凭证）
# MB_AUTH_USERNAME=现有账号
# MB_AUTH_PASSWORD=现有密码
